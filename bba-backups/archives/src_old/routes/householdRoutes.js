// backend/src/routes/householdRoutes.js
import express from "express";
const router = express.Router();

// Placeholder for Household routes
router.get("/", (req, res) => {
  res.json({ message: "Household routes working (placeholder)" });
});

export default router;
